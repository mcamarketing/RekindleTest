# =============================================================================
# REKINDLE Backend Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to git!

# Node Environment
NODE_ENV=development
PORT=3001

# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Get your Supabase credentials:
#    - Go to https://app.supabase.com/project/_/settings/api
#    - Copy the "Project URL" to SUPABASE_URL
#    - Copy the "anon public" key to SUPABASE_ANON_KEY
#    - Copy the "service_role" key to SUPABASE_SERVICE_ROLE_KEY
#
# 3. Configure CORS:
#    - Add your frontend URL to CORS_ORIGIN
#    - For production, use your actual domain (e.g., https://rekindle.com)
#
# 4. Save the .env file and restart your backend server
#
# =============================================================================
# SECURITY NOTES
# =============================================================================
#
# - NEVER commit .env files to git
# - The .env file is already in .gitignore
# - Service role key grants admin access - keep it secret!
# - Rotate keys regularly for better security
# - Use different keys for development and production
#
